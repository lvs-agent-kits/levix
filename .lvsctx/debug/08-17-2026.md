## 暂时屏蔽一个 BUG
暂时遮蔽了一个 BUG，问题出现在 copilotSystemNotification 这个 ts 文件中。
event.data.kind 的类型是 any，而非一个字符串字面量联合类型，那么 softAssertNever 就一定会报错。
但要改好的话，要改 @github//copilot-sdk 里面的内容，它似乎是在 node_modules 中，我在另一台主机上 git clone 这个项目，报错还是会有。
我要归档这个 @github//copilot-sdk，这东西的名字也不像是一个通用的插件，anyway.
